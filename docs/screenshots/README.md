# Screenshots

Add these before submitting the repo — visuals matter enormously for a quick evaluation:

1. `chat-panel.png` — the desktop app's chat interface mid-conversation
2. `vscode-diagnostics.png` — VS Code showing AI squiggles + Problems panel
3. `vscode-chat.png` — the sidebar chat panel in VS Code
4. `inline-completion.gif` — a short screen recording of ghost-text completion
5. `architecture-diagram.png` — optional: a cleaner visual version of the ASCII diagram in the README

Reference them in the main README like:
```markdown
![Chat panel](docs/screenshots/chat-panel.png)
```

A README with real screenshots gets read; a README without them gets skimmed.
